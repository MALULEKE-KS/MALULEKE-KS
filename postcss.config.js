// Tailwind v4 — vendor prefixing is handled internally (Lightning CSS),
// autoprefixer is no longer needed as a separate PostCSS plugin.
module.exports = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
