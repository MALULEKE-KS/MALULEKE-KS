// app/(admin)/admin/systems/[id]/page.tsx
// Detail/edit form matching SystemUpdateInputSchema. Publish toggle disabled
// with an inline reason (not a silent 409) when BR-1.1 blocks it.
// TODO: implement — see docs/PAGE-SPECIFICATIONS.md ("/admin/systems/[id]").

export default function AdminSystemDetailPage() {
  return null;
}
