// app/(public)/systems/[slug]/page.tsx
// /systems/[slug] — case study. Unknown/unpublished slug renders the same
// generic 404 as a real not-found (never a distinct "private" message — BR-1.3/1.4).
// TODO: implement — see docs/PAGE-SPECIFICATIONS.md ("/systems/[slug] — Case Study").

export default function SystemDetailPage() {
  return null;
}
