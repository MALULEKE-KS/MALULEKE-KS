// app/(public)/contact/_components/InquiryForm.tsx
// Colocated — only used on /contact. Real <select> for the six InquiryType
// values, client-side validated against InquiryCreateInputSchema before submit,
// hidden honeypot field, idempotencyKey generated client-side (BR-2.6).
// TODO: implement — see docs/PAGE-SPECIFICATIONS.md ("/contact").

export function InquiryForm() {
  return null;
}
