// app/(public)/page.tsx
// / — lens-aware home. LedgerHero + priority systems grid (VisitorLens-ordered)
// + curated "by the numbers" strip + How-I-Build teaser + contact band.
// TODO: implement — see docs/PAGE-SPECIFICATIONS.md ("/ — Home").

export default function HomePage() {
  return null;
}
