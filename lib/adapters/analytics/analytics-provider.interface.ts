// lib/adapters/analytics/analytics-provider.interface.ts
// Ports & Adapters (EXT-1) — the Event table + this interface are the only
// things core logic talks to; the vendor SDK stays behind the adapter.
// TODO: define the interface — see docs/PLATFORM-CONSTITUTION-v1.md §8.

export {};
