// lib/flags.ts
// Reads the Flag table — every Tier 2 agent tool and gated capability checks
// here first (BR-4.4: new tools ship disabled, opt-in only). Also honors
// FEATURE_FLAGS_KILL_SWITCH (.env.example) as an emergency override.
// TODO: implement — see docs/PLATFORM-CONSTITUTION-v1.md §1 (EXT-1, Flag table).

export {};
